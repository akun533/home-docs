version: '3.8'

services:
  home-docs:
    build:
      context: .
      dockerfile: Dockerfile
    image: home-docs:latest
    container_name: home-docs-app
    ports:
      - "8080:80"
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - ALLOWED_ORIGINS=http://localhost:8080,http://localhost
    volumes:
      # 持久化数据库文件
      - ./data:/app/server/db
      # 开发环境：挂载文档目录实现热更新（生产环境请注释掉）
      # - ./docs:/app/docs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s

# 数据卷
volumes:
  data:
    driver: local

# 运行环境 (development/production)
NODE_ENV=development

# 后端服务端口
PORT=43000

# 允许的跨域来源（多个用逗号分隔）
# 如果使用 Nginx 转发，通常不需要端口号
ALLOWED_ORIGINS=http://localhost,http://localhost:80,http://47.108.150.157,http://127.0.0.1
